{ // -*- grammar-ext: json -*-
  // metaGUI container proxy config (safe to include)
  "gui" : { "itemGrid" : { "type" : "itemgrid", "dimensions" : [0, 0], "spacing" : [0, 0] } }, // bare minimum to not crash
  "scripts" : [ "/metagui/containerstub.lua" ]
}
